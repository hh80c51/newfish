spring:
  application:
    name: eureka-provider
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    #是否将自己的Ip注册到Eureka Server，否则，将hostname注册到Eureka Server
    prefer-ip-address: true